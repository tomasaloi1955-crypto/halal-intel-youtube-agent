name: autopilot10
on: { workflow_dispatch: {} }
jobs:
  a:
    runs-on: ubuntu-latest
    steps:
      - env: { K: "${{ secrets.PEXELS_API_KEY }}" }
        run: echo "pexels=${#K}"
  b:
    runs-on: ubuntu-latest
    steps:
      - env: { K: "${{ secrets.PIXABAY_API_KEY }}" }
        run: echo "pixabay=${#K}"
